appenders:
  file:
    kind: file
    path: "log/lua.log"
    encoder:
      pattern: "{d} [{h({t})}-{L}] {M} - {m}{n}"

root:
  level: debug
  appenders:
    - file

loggers:
  cursive_core:
    level: OFF
    appenders:
      - file
